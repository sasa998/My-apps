export const onNameChange = (number) => {
  return {
      type: 'ON_NAME_CHANGE',
      payload: number
  }
}

export const onAdressChange = (number) => {
  return {
      type: 'ON_ADRESS_CHANGE',
      payload: number
  }
}